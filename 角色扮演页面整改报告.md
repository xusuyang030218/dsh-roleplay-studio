# DSH 角色扮演页面整改报告

> 版本：v1.0  
> 检查对象：DSH Web 右上角“🎭 角色扮演”入口及其 `/roleplay` 页面  
> 当前实现归属：`@furongjun1999/dsh-memory` 内置角色扮演网页，不是独立的 `dsh-roleplay` 插件。

## 一、整改目标

本次整改的目标不是简单换颜色，而是把当前“能打开但不一定能用”的演示型页面，改造成一个明确、稳定、可恢复、易学习的角色扮演工作区：

1. 用户一眼知道当前角色、当前会话和服务状态。
2. 后端未启用、网络失败、保存失败时，页面必须解释原因并给出下一步。
3. 发送、切换角色、编辑设定、翻译和历史加载不能互相覆盖或丢数据。
4. 入口与 DSH 原生界面统一，不遮挡、不突兀、不依赖脆弱的固定像素位置。
5. 视觉设计服务于长时间对话：低干扰、清晰层级、舒适阅读，而不是堆叠装饰。

## 二、现状诊断

### 2.1 入口与能力状态脱节（P0）

右上角入口由 `dsh-memory` 注入首页。插件配置中的 `capability.enabled` 默认关闭，但角色生成接口仍需要角色扮演能力后端。结果是页面能打开，发送后才暴露“后端未启用”的问题。

**整改：**

- 启动时探测角色生成能力并记录状态：可用、启动中、不可用。
- 能力不可用时隐藏入口或显示禁用态，不允许用户进入无效流程。
- 页面顶部显示状态胶囊：`服务正常`、`正在连接`、`角色生成未启用`。
- 不把内部异常、堆栈和 `bridge` 空引用直接暴露给用户。

### 2.2 错误反馈不可操作（P0）

当前页面大量直接调用 `fetch().then(x => x.json())`，没有统一检查 HTTP 状态和 `ok` 字段；对话失败后只显示“网络错误”或“无回应”。

**整改：**统一错误模型：

```json
{
  "ok": false,
  "error": {
    "code": "CAPABILITY_UNAVAILABLE",
    "message": "角色生成服务未启用",
    "hint": "请在 DSH Profile 中开启角色扮演能力"
  },
  "requestId": "..."
}
```

前端提供：`重试`、`保留草稿`、`查看诊断`，而不是让用户猜原因。

### 2.3 发送流程会造成“假成功”（P0）

用户消息在请求成功前就被加入聊天区，输入内容也被清空；角色编辑保存后，无论导入是否真正成功，都可能提示“已保存”。

**整改：**引入状态机：

- 用户消息：`草稿 → 发送中 → 已发送 / 失败`
- 保存操作：`未保存 → 保存中 → 已保存 / 保存失败`
- 失败时保留输入和编辑内容，并提供一键重试。

### 2.4 角色切换存在异步竞态（P0）

快速从角色 A 切换到 B 再切换到 C 时，旧请求可能晚返回并覆盖当前内容。

**整改：**使用 `AbortController`、请求序列号和当前 `roleId` 校验；切换期间显示骨架屏，旧结果一律丢弃。

### 2.5 页面是功能集合，不是工作区（P1）

当前顶部同时放置角色选择、新建、详情、翻译，缺少会话管理、搜索、清空、导出、复制、重试、上下文状态、角色状态等核心能力。

**整改：**采用“角色导航 + 对话主区 + 设置抽屉”的工作区结构。

## 三、信息架构与布局方案

### 3.1 桌面端布局

```text
┌─────────────────────────────────────────────────────────────┐
│ 返回 DSH  | 角色扮演 | 当前角色 | 服务状态 | 更多操作        │
├───────────────┬───────────────────────────────┬─────────────┤
│ 角色导航       │ 对话主区                      │ 设置抽屉/面板 │
│               │                               │             │
│ 搜索角色       │ 角色头像 + 名称 + 场景摘要       │ 人设         │
│ 新建角色       │                               │ 记忆         │
│ 导入/导出       │ 消息列表                      │ 锚点         │
│ 角色列表       │                               │ 价值观       │
│ 最近会话       │ 上下文/召回信息                │ 世界观       │
│               │ 输入框 + 发送/停止              │ 翻译         │
└───────────────┴───────────────────────────────┴─────────────┘
```

建议宽度：

- 左侧导航：240–280px，可折叠。
- 中间主区：最小 560px，最大 860px，保证长文本阅读宽度。
- 右侧设置：320–380px，默认隐藏，打开时不挤压消息区，可改为抽屉。
- 内容区最大宽度：1200–1440px，避免超宽屏导致消息行过长。

### 3.2 移动端布局

- 顶部只保留返回、当前角色、状态和更多菜单。
- 角色列表、设置、翻译改为底部抽屉或侧滑面板。
- 输入栏固定在底部，适配 `safe-area-inset-bottom`。
- 发送按钮至少 44×44px；输入框最小高度 46px。
- 不使用固定 `top/right` 浮动按钮遮挡原生导航。

### 3.3 入口位置

取消当前硬编码的首页右上角超高层级浮动按钮：

```css
position: fixed;
right: 14px;
 z-index: 999999;
```

优先使用 DSH 官方扩展点注册导航项。如果只能注入 HTML，则放入统一的工具入口容器，并遵循：

- 不遮挡原生标题栏、侧边栏和窗口控制区。
- 入口不默认拖动；拖动不是发现性强的核心交互。
- 需要移动时提供“恢复默认位置”。
- 入口文字采用“角色扮演”而不是只依靠表情图标。
- 移动端改为菜单项，不继续固定在右上角。

## 四、视觉设计规范

### 4.1 设计方向

关键词：**安静、沉浸、清晰、可恢复**。

角色扮演是长时间阅读和输入场景，应减少高饱和颜色、过重阴影和无意义动画。界面要像一个可靠的编辑工作区，而不是营销卡片。

### 4.2 色彩系统

建议使用语义化 CSS 变量，不把颜色硬编码在 HTML 字符串中。

#### 深色主题（默认）

| 用途 | 色值 | 说明 |
|---|---|---|
| 页面背景 | `#0F1115` | 低对比深色底，减少眩光 |
| 一级面板 | `#171A21` | 导航、设置、输入区 |
| 二级面板 | `#1E2330` | 卡片、浮层、角色信息 |
| 分割线 | `#2B3240` | 只用于结构分隔 |
| 主文字 | `#F2F4F8` | 长文本阅读 |
| 次文字 | `#A8B0BF` | 辅助说明、时间、状态 |
| 主强调色 | `#8B9CFF` | 选中、主要按钮、链接 |
| 成功色 | `#65D6A5` | 服务正常、保存成功 |
| 警告色 | `#F2C46D` | 连接中、需确认 |
| 错误色 | `#F07F87` | 失败、拒绝、危险操作 |

主强调色应只占界面面积约 5%–10%，避免所有按钮都使用高亮色。

#### 浅色主题

| 用途 | 色值 |
|---|---|
| 页面背景 | `#F7F8FA` |
| 一级面板 | `#FFFFFF` |
| 分割线 | `#E3E7EE` |
| 主文字 | `#1B2230` |
| 次文字 | `#667085` |
| 主强调色 | `#5F70D8` |

### 4.3 颜色语义

- 紫蓝色只表示“当前选择/主要操作”，不要同时表示错误和链接。
- 绿色只表示真实成功，不表示“请求已发出”。
- 黄色表示等待或需要用户确认。
- 红色只表示失败、拒绝或危险操作。
- 成人内容提示使用明确的警示色，但不应让普通角色页面处于持续警告状态。

### 4.4 字体、间距与圆角

- 中文优先：`"PingFang SC", "Microsoft YaHei", system-ui, sans-serif`。
- 正文 14–15px，消息行高 1.7–1.8。
- 页面标题 16–18px，辅助文字 12–13px。
- 基础间距 4px，常用间距 8/12/16/24px。
- 面板圆角 12px，输入框 10px，按钮 8px。
- 阴影只用于浮层：`0 12px 32px rgba(0,0,0,.24)`；普通面板用边框，不要层层叠加阴影。

## 五、交互整改

### 5.1 首次进入

1. 先显示角色生成服务状态。
2. 如果服务不可用，直接显示原因、配置位置和诊断按钮。
3. 有可用角色时显示角色列表和一句场景说明。
4. 没有角色时显示清晰的空状态：`创建第一个角色`、`导入角色卡`。
5. 成人确认只对标记为 NSFW 的角色触发，不要无差别阻断普通角色。

### 5.2 发送与生成

- 支持 Enter 发送、Shift+Enter 换行，并提供设置项。
- 发送期间显示“正在生成”，提供停止按钮。
- 失败消息显示“重试”，并保留用户草稿。
- 每条消息支持复制；角色回复支持重新生成。
- 用户消息在服务确认后标记为已发送；失败消息不能伪装成成功。
- 长回复采用逐步显示或明确的生成进度，不要长时间无反馈。

### 5.3 角色管理

- 角色列表支持搜索、排序、最近使用。
- 新建角色使用分步表单：基本信息 → 人设 → 首条消息 → 内容分级。
- 详情页分为“人设、记忆、锚点、价值观、世界观、翻译”。
- 明确“保存当前页”和“保存全部”的区别。
- 保存失败时不关闭面板，不丢失未保存内容。
- 增加导入/导出角色卡，至少支持 JSON；后续再扩展 PNG 卡片。

### 5.4 会话管理

- 角色和会话分离：一个角色可以有多个会话。
- 支持新建会话、重命名、归档、删除和导出。
- 提供“新章节”操作，自动摘要旧上下文。
- 显示上下文使用量、召回记忆数量和当前会话时间。

## 六、后端与数据整改

### 6.1 API 统一

所有接口统一返回 `ok`、`error.code`、`error.message`，并正确返回 4xx/5xx 状态码。前端不得把 500 当作正常 JSON 继续渲染。

### 6.2 数据模型

不要把所有角色资料、翻译和条目长期耦合在一个 `_roles.json`。建议迁移为：

```text
roleplay_data/
├── index.json
├── roles/<role-id>.json
├── sessions/<session-id>.jsonl
├── translations/<role-id>.json
└── exports/
```

增加 `schema_version`、`updated_at`、写入队列和备份恢复机制。并发保存时使用串行写入或文件锁，避免 read-modify-write 互相覆盖。

### 6.3 上下文管理

“完整转录”不等于“模型无限记忆”。需要加入：

- 自动摘要。
- 会话分段。
- 关键设定固定注入。
- 上下文窗口统计。
- 召回记忆数量显示。
- 新章节入口。

## 七、源码与工程整改

1. 清理 `lib/roleplay_web.js` 与 `lib/lib/roleplay_web.js` 的重复实现，明确唯一源码和构建产物。
2. 删除重复的 `@ts-nocheck` 注释和过期架构注释。
3. 不在大段 HTML 字符串中继续堆叠业务逻辑；逐步拆出模板、样式、客户端状态和 API 层。
4. 使用统一的 `requestJson()` 封装请求、超时、错误码和重试。
5. 为角色切换、消息发送、保存、服务未启动、并发写入增加自动化测试。
6. 更新 README、配置示例和实际版本号，去掉旧 AEIS 架构的混淆说明。
7. 增加可观测性：请求 ID、角色 ID、会话 ID、耗时、失败原因，但不要记录敏感正文到普通日志。

## 八、验收标准

### P0 功能验收

- 后端关闭时入口不会引导用户进入无效页面。
- 后端未启动时，页面显示可理解的配置提示。
- 发送失败不丢输入，点击重试可以继续。
- 保存失败不显示“已保存”，编辑内容仍保留。
- 快速切换角色不会出现历史串页。
- 多浏览器客户端不会读取彼此的会话转录。

### P1 体验验收

- 新用户 30 秒内能创建角色并发送第一条消息。
- 用户能找到当前角色、当前会话、服务状态和设置入口。
- 主要操作无需依赖浏览器原生 `alert`。
- 桌面端无入口遮挡；移动端输入栏不被键盘遮挡。
- 深色与浅色主题对比度满足可读性要求。

### P2 稳定性验收

- 连续 20 轮对话无消息错序、重复或丢失。
- 100 轮压力测试后页面仍可滚动、输入和切换角色。
- 中断、超时、刷新、重启后会话可恢复。
- 角色数据损坏时只影响单个角色，不导致整个角色列表不可用。

## 九、实施优先级

### 第一阶段：止血（1–2 天）

- 绑定入口和 capability 状态。
- 统一 API 错误处理。
- 修复发送、保存、切换竞态。
- 添加重试和保留草稿。
- 移除 `z-index: 999999` 的浮动入口方案。

### 第二阶段：重做工作区（3–5 天）

- 实现角色导航、对话主区、设置抽屉。
- 增加会话管理、复制、导出、清空、重新生成。
- 完成颜色、间距、字体、响应式布局规范。
- 优化移动端输入体验。

### 第三阶段：数据与工程治理（2–4 天）

- 角色/会话/翻译数据拆分。
- 增加 schema 版本和迁移。
- 清理重复构建产物。
- 补齐自动化测试、日志和文档。

## 十、最终结论

当前最大问题不是“页面不够漂亮”，而是产品状态不透明：入口存在不代表服务可用，按钮反馈不代表保存成功，历史存在不代表模型真正使用了全部上下文。整改必须先建立“状态真实、失败可解释、操作可恢复”的基础，再进行布局和视觉升级。

最终形态应当是：**入口不打扰，状态看得懂，对话不中断，失败能恢复，设定可管理，长聊不迷路。**

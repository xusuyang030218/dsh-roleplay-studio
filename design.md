# 角色扮演工作区第一阶段设计

## 目标

直接改善当前 DSH 运行中的 `/roleplay` 页面，优先解决“入口能打开但状态不透明、失败不可恢复、页面不像工作区”的问题，不在本阶段重做后端数据模型。

## 方案

采用“保留现有 API + 重构单页壳层”的渐进方案：

1. 保留现有角色、历史、翻译、详情 API，降低回归风险。
2. 将页面视觉改为三层结构：顶部工作区栏、角色信息/状态栏、对话主区与输入区；设置仍用现有弹层，先统一样式。
3. 所有请求通过前端统一包装器处理 HTTP 状态、JSON 错误和超时。
4. 消息发送采用 pending/failed 状态；失败保留草稿并提供重试。
5. 角色切换使用请求序列号，旧历史响应不得覆盖新角色。
6. 保存操作显示保存中/成功/失败，失败时不关闭面板。
7. 增加服务状态提示；生成后端不可用时在页面明确提示，而不是显示“无回应”。

## 视觉规范

默认深色低干扰主题：背景 `#0f1115`，面板 `#171a21`，输入/二级面板 `#1e2330`，边框 `#2b3240`，主文字 `#f2f4f8`，次文字 `#a8b0bf`，强调色 `#8b9cff`，成功 `#65d6a5`，警告 `#f2c46d`，错误 `#f07f87`。使用 8/12/16/24px 间距体系、12px 面板圆角、44px 最小操作高度。

## 验收

- 页面打开后能看到当前角色和服务状态。
- 接口返回 4xx/5xx 时显示可理解错误，且不丢输入。
- 发送失败可重试；保存失败不会提示成功。
- 快速切换角色不会串历史。
- 桌面端不依赖超高 z-index 浮动入口；移动端输入区可用。
- 修改后的运行文件可通过 Node 语法检查，并同步到个人仓库保存。
